fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.27.2
Pillow==10.4.0
numpy>=1.26.5
opencv-python-headless==4.10.0.84
boto3==1.35.36
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Face Recognition (InsightFace ArcFace) ───────────────────
insightface>=0.7.3
onnxruntime>=1.17.0
requests>=2.31.0

# ── Cloud humanization (optional, no GPU needed) ─────────────
# Set REPLICATE_API_KEY in .env to activate — free at replicate.com
replicate>=0.25.0

# ── SAM setup (needed by scripts/setup_sam.py) ───────────────
gdown>=4.7.3

# ── GPU humanization (CUDA laptop only) ──────────────────────
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.11.0+cu130
torchvision==0.26.0+cu130
diffusers>=0.37.1
transformers>=5.6.2
accelerate>=1.13.0
safetensors>=0.7.0
huggingface-hub>=1.12.0
controlnet-aux>=0.0.10

# The service auto-detects CUDA at startup — no config needed.
# First generate call downloads ~5 GB of models to ./models/
