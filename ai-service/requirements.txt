fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.27.2
Pillow==10.4.0
numpy>=1.26.5
opencv-python-headless==4.10.0.84
boto3==1.35.36
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Face Recognition (InsightFace ArcFace) ───────────────────
insightface>=0.7.3
onnxruntime>=1.17.0
requests>=2.31.0

# ── Cloud humanization (optional, no GPU needed) ─────────────
# Set REPLICATE_API_KEY in .env to activate — free at replicate.com
replicate>=0.25.0

# ── SAM setup (needed by scripts/setup_sam.py) ───────────────
gdown>=4.7.3

# ── SAM face alignment (FFHQ-style, needed for good aging results) ───────────
# dlib requires cmake: pip install cmake  then  pip install dlib
# If install fails, the service falls back to InsightFace bbox crop
scipy>=1.11.0
dlib>=19.24.0

# ── GPU humanization (CUDA laptop only) ──────────────────────
# PyTorch + CUDA must be installed BEFORE this file with the right CUDA version.
# Run ONE of these in the venv first, then pip install -r requirements.txt:
#
#   CUDA 12.1 (recommended — works on most recent NVIDIA GPUs):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#   CUDA 11.8 (older GPUs / older drivers):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Verify CUDA works:
#   python -c "import torch; print(torch.cuda.is_available(), torch.version.cuda)"
#
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.21.0
transformers>=4.36.0
accelerate>=0.24.0
safetensors>=0.4.0
huggingface-hub>=0.20.0
controlnet-aux>=0.0.7

# The service auto-detects CUDA at startup — no config needed.
# First generate call downloads ~5 GB of models to ./models/
