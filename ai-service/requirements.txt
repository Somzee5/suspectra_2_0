fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.27.2
Pillow==10.4.0
numpy>=1.26.5
opencv-python-headless==4.10.0.84
boto3==1.35.36
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Face Recognition (InsightFace ArcFace) ───────────────────
insightface>=0.7.3
onnxruntime>=1.17.0
requests>=2.31.0

# ── Cloud humanization (optional, no GPU needed) ─────────────
# Set REPLICATE_API_KEY in .env to activate — free at replicate.com
replicate>=0.25.0

# ── SAM setup (needed by scripts/setup_sam.py) ───────────────
gdown>=4.7.3

# ── SAM runtime dependencies ─────────────────────────────────
# lpips: required by SAM's models/psp.py (perceptual loss used in encoder)
# ninja: required by StyleGAN2 custom CUDA ops (JIT compilation at first load)
lpips>=0.1.4
ninja

# ── SAM face alignment (FFHQ-style, needed for good aging results) ───────────
# dlib requires cmake: pip install cmake  then  pip install dlib
# If install fails, the service falls back to InsightFace bbox crop
scipy>=1.11.0
dlib>=19.24.0

# ── SD / ControlNet (GPU humanization, installed via install_gpu script) ──────
# torch and torchvision are intentionally NOT listed here — pip resolves them
# to the CPU wheel from PyPI and ignores the CUDA index.
# Use install_gpu.bat (Windows) or install_gpu.sh (Linux/Mac) instead.
diffusers>=0.21.0
transformers>=4.36.0
accelerate>=0.24.0
safetensors>=0.4.0
huggingface-hub>=0.20.0
controlnet-aux>=0.0.7

# The service auto-detects CUDA at startup — no config needed.
# First generate call downloads ~5 GB of models to ./models/
