fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
python-dotenv==1.0.1
httpx==0.27.2
Pillow==10.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
boto3==1.35.36
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Face Recognition (InsightFace ArcFace) ───────────────────
insightface>=0.7.3
onnxruntime>=1.17.0
requests>=2.31.0

# ── Cloud humanization (optional, no GPU needed) ─────────────
# Set REPLICATE_API_KEY in .env to activate — free at replicate.com
replicate>=0.25.0

# ── SAM setup (needed by scripts/setup_sam.py) ───────────────
gdown>=4.7.3

# ── GPU humanization (CUDA laptop only) ──────────────────────
# Run these manually BEFORE pip install -r requirements.txt:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install diffusers>=0.27.2 transformers>=4.38.0 accelerate>=0.28.0
#   pip install safetensors>=0.4.2 huggingface-hub>=0.21.0 controlnet-aux>=0.0.9
#
# The service auto-detects CUDA at startup — no config needed.
# First generate call downloads ~5 GB of models to ./models/
